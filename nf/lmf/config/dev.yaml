nf_instance_id: "00000000-0000-4012-8000-000000000001"

plmn:
  mcc: "001"
  mnc: "01"

sbi:
  address: "0.0.0.0:8012"
  fqdn: "lmf.5gc.mnc001.mcc001.3gppnetwork.org"
  tls:
    cert_file: /etc/5gc/pki/lmf.crt
    key_file: /etc/5gc/pki/lmf.key
    ca_file: /etc/5gc/pki/ca.crt

peers:
  nrf: "nrf:8000"
  amf: "amf:8001"

metrics:
  address: "0.0.0.0:9113"

# cell_coordinates is an optional map from PLMN+NRCellId to WGS84 coordinates.
# When a match is found the DetermineLocation response locationEstimate uses the
# configured lat/lon; absent entries yield lat=0, lon=0 (placeholder).
# Ref: TS 29.572 §6.1.6.2.2 (locationEstimate, GeographicArea shape=POINT).
cell_coordinates: {}
